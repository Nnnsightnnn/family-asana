node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# vite cache
*.timestamp-*.mjs

# data
server/data/
server/*.db
server/*.db-journal
server/*.db-wal
server/*.db-shm
server/uploads/

# local SQLite backups (defensive — admin.sh backup-now writes here if APP_DIR=$(pwd))
backups/
server/backups/

# Personal AI context — keep out of the cloud repo
CLAUDE.md
.claude/

# Operational / deploy notes — local-only, not part of the codebase
docs/

